.env
keys.json
config.json
node_modules
logs
.DS_Store
.github
.claude
CLAUDE.md
mod_inspector_cache.json
verdict_store.json
crash_risk_cache/

# SQLite database files
*.db
*.db-shm
*.db-wal

# Legacy data files (migrated to SQLite)
users.json
blacklist.json

test-docs/

# Regenerable verification run artifacts (Tier 1/Tier 2 output, eval sweeps).
# These are several hundred KB each and are reproduced by re-running the script,
# so they are outputs to inspect locally rather than source to review.
docs/live-modpack-matrix*
docs/modpack-smoke-*
docs/modpack-corpus-phase*-summary.json
